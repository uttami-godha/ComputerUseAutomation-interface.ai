{
  "name": "computer-use-automation",
  "version": "0.1.0",
  "private": true,
  "description": "Record-once / replay-many computer-use automation for legacy applications.",
  "type": "module",
  "engines": {
    "node": ">=22.6.0"
  },
  "scripts": {
    "mock": "node src/mock-app/server.ts",
    "discover": "node src/cli.ts discover",
    "replay": "node src/cli.ts replay",
    "capabilities": "node src/cli.ts capabilities",
    "schema": "node src/cli.ts schema",
    "test": "node tests/harness.ts",
    "gen-evidence": "node tests/gen-evidence.ts",
    "gen-real-evidence": "bash scripts/generate-real-evidence.sh",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "playwright": "^1.52.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}